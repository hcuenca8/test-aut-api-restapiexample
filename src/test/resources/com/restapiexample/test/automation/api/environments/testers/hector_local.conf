{

  # Ambiente local
  hector.cuenca.api.local {

    # Aplicacion api sujeta a pruebas
    api.url{
      base = "https://dummy.restapiexample.com/api"

      path{
        get_employee = "/v1/employee"
        get_employees = "/v1/employees"
        pst_create = "/v1/create"
        put_update = "/v1/update"
        del_delete = "/v1/delete"
      }
    }
  }
}
